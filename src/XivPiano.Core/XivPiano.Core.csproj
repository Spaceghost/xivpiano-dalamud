<Project Sdk="Microsoft.NET.Sdk">
  <!-- Pure library: the Pandora JSON API (Blowfish, login, stations, playlists, feedback) and the radio queue. BCL only, no Dalamud, no audio. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>XivPiano.Core</RootNamespace>
    <AssemblyName>XivPiano.Core</AssemblyName>
    <Version>0.1.0.0</Version>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="XivPiano.Core.Tests" />
  </ItemGroup>
</Project>
